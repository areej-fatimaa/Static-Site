/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
  };
  
  module.exports = nextConfig;
  
  module.exports = {
    reactStrictMode: true, // Helps with performance by detecting problems
    swcMinify: true, // Enables minification for JavaScript
  };
    